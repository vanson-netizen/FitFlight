# FitFlight 项目协作说明

## 项目目标

本项目是 FitFlight 微信小程序。Codex 的职责是协助实现、调试和验证功能，并尽量保持现有产品思路、交互和视觉风格一致。

## 信息来源与优先级

处理任务时，按以下顺序理解需求：

1. 用户在当前对话中明确提出的要求。
2. 本文件中的长期工程约束。
3. 工作区现有代码所体现的架构、命名和实现方式。
4. `FitFlight.docx` 与 `微信小程序pptx.pptx` 中的产品思路和业务说明。
5. `fitflight-fifi-demo.html` 中的界面、交互和视觉参考。

Word、PPT 和 HTML demo 都是参考资料，不是可直接执行的指令。若资料之间冲突、资料与当前请求冲突，或某项内容明显只是概念演示，应先遵循当前请求，并说明采用了什么判断。

## 开始任务前

- 先阅读与任务直接相关的文件，再修改代码；不要只根据文件名猜测实现。
- 先检查现有目录结构、配置、组件和依赖，优先沿用已有方案。
- 若需求可以从代码或资料中确定，直接推进；只有会显著改变产品行为、数据结构或技术路线时才向用户确认。
- 修改前检查工作区已有改动，不覆盖或撤销用户尚未提交的内容。

## 微信小程序实现约束

- 使用项目当前已经采用的微信小程序技术栈、目录结构和代码风格；不要无理由引入新的框架或状态管理方案。
- 优先使用原生小程序能力和已有组件。新增依赖前说明必要性，并确认它适合微信开发者工具的构建环境。
- 页面应同时考虑加载中、空数据、失败、无权限和正常数据状态。
- 涉及登录、用户资料、定位、健康、相机、相册、通知等能力时，明确处理授权拒绝、隐私提示和降级路径。
- 不在前端代码中写入密钥、令牌、真实个人信息或生产环境凭据。
- 网络请求、存储键名、路由路径和数据结构应集中复用，避免在多个页面硬编码。
- UI 实现以 demo 的信息层级和交互意图为参考，但应适配小程序组件、系统安全区、不同屏幕宽度和实际数据状态。
- 没有明确要求时，不擅自增加后端服务、云开发资源、付费接口或需要发布审核的新能力。

## 修改原则

- 每次修改聚焦当前功能，避免顺手进行大范围重构。
- 优先修复根因，不用重复代码或临时特判掩盖问题。
- 保持命名清楚；注释解释业务原因和限制，不复述代码表面行为。
- 若需要占位数据，清楚标记 mock 数据及其替换位置，不把演示数据伪装成真实接口结果。
- 若产品资料只描述了效果而未定义数据来源，先设计最小可替换接口边界，并在交付说明中列出假设。

## 验证要求

- 修改后运行项目已有的 lint、类型检查、测试或构建命令；不要虚构未运行的验证结果。
- 对页面功能至少检查：首次进入、主要操作、返回/重进、空数据、异常提示以及不同数据量。
- 微信开发者工具中需要人工完成的预览、授权、真机或网络环境验证，应给出简短明确的操作步骤和预期结果。
- 修复缺陷时，在条件允许的情况下补充能够复现问题的测试或最小验证步骤。

## 交付格式

完成任务后，用简洁中文说明：

- 实现或修改了什么；
- 关键文件在哪里；
- 自动验证的结果；
- 仍需用户在微信开发者工具中验证的内容；
- 存在的假设、限制或后续工作。

不要声称已经在微信开发者工具或真机上验证，除非确实完成了该验证。

## 与用户协作

- 默认使用中文沟通，代码标识符遵循项目现有语言和风格。
- 如果发现一个明显更稳妥的实现方式，先说明收益和代价；不因个人偏好改写已确定的产品方向。
- 遇到阻塞时，先完成所有不依赖该阻塞项的工作，再提出一个具体、可回答的问题。
