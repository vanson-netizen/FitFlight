# 当前项目状态

最后更新：2026-09-02

## 当前阶段

北航培养资源与保守通用方案生成链路已完成本地实现，等待微信开发者工具及云环境验收；健康规则和资源数据仍处于待审核状态。

## 当前可运行状态

- [ ] 已注册小程序
- [ ] 已取得 AppID
- [ ] 已创建小程序项目
- [ ] 已开通云开发环境
- [x] 首页可以编译
- [ ] 可以真机预览

## 已完成

- 2026-09-05 后续修复：用户已确认减脂餐原图对应合一餐厅二楼，压缩副本现已按 food-xueyuan-option-bd51c94c2f 接入，蔓味轻食封面不变。首页三张入口改为按实际可用视口统一计算200—230rpx高度、18rpx间距，最小屏幕可滚动；运动图使用顶部裁剪以保留人物头部。图片失败会记录路径后回退。详见 docs/COMMUNITY_COMPACT_HOME_FIX.md；前述减脂餐待确认事项已解除。

- 2026-09-05：社区图片展示接入 20 张本地压缩照片，统一 768×432（16:9），稳定资源 ID 集中映射、聚合食堂共用封面、蔓味轻食使用独立商家封面、双羽毛球馆分别绑定主馆／副馆。所有最终图片已逐张查看，原图哈希不变，图片加载失败回退已实现；17 个测试脚本和项目结构检查通过。减脂餐照片有两个同名父实体，待用户确认后绑定；开发者工具多宽度及真机画质／加载验收仍待完成，详见 docs/COMMUNITY_IMAGES.md。本次未修改云函数、数据库或其他业务。

- 已完成北航学生整理 CSV 的本地 dry-run 接入代码：UTF-8 BOM、列数、稳定 ID、日期、数组去重、校区/分类、餐厅外键、热量清空和价格区间均有校验；方案和社区已改为通过统一资源 repository 读取三个目标集合，数据库读取失败时方案继续生成通用内容。本批 dry-run 得到 9 条场地、10 条餐厅/商家和 4 条可关联菜品；蔓味轻食按 `external_merchant` 建模并以校区加名称关联，全部记录保持 `pending_verification`，尚未创建集合或写入云数据库。

- 已本地实现 FIFI 轻量陪伴页面第一版：只读取当前有效培养方案与既有逐日三项打卡，确定性推导每日/总成长值、成长阶段、连续培养天数和最新成就，不建立宠物数据库、不保存派生积分。页面具有 loading、empty、error、success 四态、三种本地可替换小狗形象、无图降级、温和对话切换和返回重读；底部 FIFI 入口已从“我的”“培养”“社区”接通，尚待微信开发者工具布局与云端历史数据验收。

- 已本地实现社区页面第一版：社区首页提供运动、饮食、论坛三个入口；运动和饮食采用左侧分类与右侧资源列表布局，论坛仅提供静态容器和空状态。社区资源通过独立前端 repository 读取，当前版本化来源为空数组，不接云数据库、不复用 trainingPlan 资源快照、不展示未经核实的北航场馆或餐饮信息；页面预留 loading、empty、error、success 和 `pending_verification/verified/expired` 状态。底部社区入口已从“我的”和“培养”页面接通，尚待微信开发者工具和真机布局验收。

- 已本地实现培养页锻炼、饮食、作息三项每日打卡闭环：完成状态独立保存到 `cultivation_daily_checkins`，按当前微信用户、明确 planId 和北京时间日期隔离；服务端校验方案归属、周期、dayPlan、活动状态和 revision，不修改不可变方案快照。页面提供三项顶层手动确认、取消、单项保存态、失败回滚、云端回填及实时 `0/3` 至 `3/3` 完成度；未扩展健康记录、日志、成就、提醒或真实饮食记录。
- 已修复画像目标变化后的方案确认、暂不调整、重新生成、状态切换和页面刷新闭环：旧快照保持不可变，暂不调整转 outdated，调整成功切换最新目标方案并归档旧方案，失败保留 currentPlanId；“我的方案”展示当前画像与旧方案目标差异，“培养”屏蔽不再适用于当前画像的旧任务。线上生效需重新部署 `userPortrait` 与 `trainingPlan`。
- 已本地实现 `fitflight-cultivation-v1-draft.2` 开发测试生成器：在安全闸门通过后一次生成 4 周总体摘要、四阶段周结构和 28 天同源每日快照，五类目标具有不同训练、饮食、作息与复盘侧重；规则状态保持 `product_draft`，所有待审核数值未启用。
- “我的方案”已增加周期概要、四周卡片、原则、版本及 draft 提示；“培养”继续按日期读取同一 `currentPlanId` 快照。旧方案缺少新字段时降级展示，不按新规则重算。当前北航资源仍全部为 `pending_verification`，新方案正常显示“暂无已核实地点”。
- 已完成安全信息保存、回填和返回闭环的本地修复与自动检查：`saveSafetyScreening` 在页面 service、云函数 action 分派和请求白名单中保持一致；缺项或未确认由前后端明确拒绝，失败保留输入，成功有栈返回上一页、无栈返回画像页。线上生效仍需重新部署 `userPortrait`。
- 已完成候选规则 `fitflight-cultivation-v1-draft.2` 的设计资料和 16 个匿名测试画像，覆盖当前运行草案审计、权威来源、字段映射、安全问卷缺口、安全闸门、五目标、4 周周期、总体/每日同源快照、资源边界、集中参数及第二轮最小实施顺序；候选规则仍为 draft，本轮未接入运行代码或改变现有方案。
- 已将 3 条场地、6 条食堂和 2 条菜品转换为云函数内版本化资源数据 `buaa-resource-pending.1`，全部为 `pending_verification`；运行时不读取 CSV，源文件编码、哈希和记录数保存在资源清单中。
- 已增加幂等资源导入脚本：稳定 ID 唯一、菜品食堂引用、无来源热量、列数和历史版本覆盖均会被校验；相同输入重复导入不产生重复 ID，内容变化必须创建新版本。
- 已实现只允许 `verified + verifiedAt + 未过期` 资源进入具体推荐的状态闸门。当前数据只显示待核实提示，场地降级为“暂无已核实地点”，不展示未核实价格、开放规则或热量。
- `trainingPlan` 已接入保守通用健康管理方案，使用校区、目标、运动偏好、能力标签、餐别和食物分类标签匹配资源，并把资源版本和逐日匹配结果保存为方案快照；画像存在或不确定运动限制时不生成运动方案。
- 培养页已读取当前有效方案并展示今日安排、推荐时间、推荐地点、地点状态、饮食建议、待核实状态及健康免责声明；画像已增加校区选择。
- 已完成“培养方案 V1”规则规格与数据字典草案，明确健康规则/北航资源解耦、安全闸门、三类主要目标差异、4 周周期、资源降级、不可变方案快照、审核机制及 JSON→云数据库迁移条件；未实现或修改正式生成器。
- 已在 `docs/draft/seed-data` 增加仅用于结构验证的版本化 draft/preview JSON：1 个规则集、7 种通用运动、3 个膳食结构模板，以及学院路/沙河各 1 条明确待调查的场地和食堂结构占位；未写入未经核实的开放时间、菜品、热量或营养值，未接入运行代码。
- 已完成北航培养资源调查表字段说明，覆盖场地、食堂、菜品、来源和待核实问题的填写规则及禁止推测项。
- 已新增独立“培养”页面和首页底部入口，完成日期/周期/完成度、健康记录/我的日志入口，以及锻炼、饮食、作息三张今日方案卡片的展示层；健康记录、日志、饮食记录按钮仅提示后续接入，不创建假路由或真实记录。
- 已集中定义 `plan`、`dailyRecord`、`displayModel` 三层今日方案展示契约，明确分离 `plannedExercise/exerciseRecord`、`recommendedMeals/actualMeals`、`recommendedSleep/actualSleep`；页面具备 `loading`、`empty`、`ready`、`rest`、`error` 五态。
- 今日方案 service 在 preview 开关关闭时读取 `trainingPlan` 当前有效方案；无方案或当日切片不存在时返回 `empty`。preview 仍只用于布局验收且默认关闭。

- 已实现 V1 初始用户画像本地代码：新增独立 `user_portraits` 云端边界、画像版本控制、云端 BMI/体重目标差计算、用户目标与训练/安全条件、动态过期判断、集中重大变化策略、画像引导/编辑/展示页面，并在重大变化且存在有效旧方案时仅复用 `pending_confirmation` 标记待调整；未配置或调用正式培养规则，尚待部署和微信开发者工具验收。

- 已实现首次身体信息引导、云端身体档案四态判断、“我的”页面完成态激励卡片、“我的画像”身体档案四态展示及现有身体信息页的云端回填与修改；首页不展示具体身体数据，相关页面统一使用现有 `saveBodyProfile` 云函数和 service，未实现培养方案生成。
- `saveBodyProfile` 已兼容读取与保存动作，并增加字段白名单、服务端校验、稳定错误码、`profileVersion` 乐观并发控制、无变化不更新和旧记录安全默认版本；仍需在云开发控制台部署云函数，并为 `body_profiles._openid` 建立唯一索引及设置仅管理端可读写权限。
- 已将登记后的页面顶部标题固定为“FitFlight”，并建立“我的、FIFI、培养、社区”底部导航；“我的”、FIFI、培养和社区已有页面，图标仍为可替换占位。
- 已完善登记后的“我的”首页结构，包括用户资料空状态、我的画像入口、成长趋势、我的方案、横向成就徽章及设置与隐私入口；已新增“我的画像”身体档案详情页，其余入口未新增详情页或路由。
- 已在原生小程序首页实现本地用户头像和昵称登记、登记状态判断及重新设置入口。
- 用户资料存储键和读写逻辑已集中封装，未新增云开发资源或数据库结构。
- 已准备微信开发者工具。
- 已准备使用 VS Code 编写代码。
- 已配置 Codex 工作区协作规则 `AGENTS.md`。
- 已有 FitFlight 需求文档 `FitFlight.docx`。
- 已有网页 Demo `fitflight-fifi-demo.html`。
- 已有项目展示 PPT `微信小程序pptx.pptx`。
- 已确定使用原生微信小程序和微信云开发。
- 已实现方案状态集中定义、独立方案 service、`trainingPlan` 云函数边界、首次生成/资料调整询问和“我的方案”最小状态页；生成内容是明确带免责声明的保守通用健康管理建议，不是医疗建议。
- `plan-generator.js` 仍是唯一生成入口；当前规则版本为 `fitflight-general-guidance-v1-draft.1`，资源版本和匹配结果随方案内容保存，生成新方案前不覆盖旧方案。

## 正在进行

- 在微信开发者工具中验收培养页面五态、不同屏幕宽度、长内容换行、自然滚动和底部安全区。
- 验证第二轮方案状态链路、幂等请求和失败保留旧方案行为。
- 确认 `training_plan_states`、`training_plans` 的线上集合、索引与权限。

## 下一步

1. 在微信开发者工具中完成培养页面 empty/ready/rest/error 与适配验收。
2. 在云开发控制台创建并配置 `user_portraits` 集合、唯一索引和仅管理端可读写权限。
3. 部署新增 `userPortrait` 和更新后的 `trainingPlan` 云函数。
4. 在微信开发者工具中完成画像首次生成、修改、过期、并发冲突和方案待调整联调。
5. 正式方案规则确认后，让规则引擎输出与今日方案 `plan` 契约兼容的周期内容；完成记录仍通过独立 `dailyRecord` 边界接入。

## 当前问题

- FitFlight 核心计算规则尚需确认。
- 完整 V1 安全闸门需要疼痛、损伤、术后、医生限制、孕产/特殊状态、医疗目的和进食异常等显式问卷字段；当前画像仅有运动限制状态，不能把缺失信息默认为安全。
- 4 周周期、递进、恢复间隔、目标体重决策和饮食份量表达仍需医生、体育教师和注册营养师审核后才能从 `draft` 进入 `reviewed`。
- 需要确认正式发布是否涉及特殊服务类目。
- MVP 页面范围和功能优先级尚未最终确定。
- 数据库集合、字段和权限规则尚未确定。
- 当前通用健康管理规则仍为 `draft`，尚需医生、营养师和体育教师审核；北航资源全部为 `pending_verification`，尚无可作为已核实地点或菜品展示的记录。
- 培养页面 preview 数据只用于本地布局验收；正式周期方案内容、每日切片规则和真实完成记录持久化均未实现。

## 最近一次验证

2026-08-31 已完成北航资源接入本地自动检查：三份 CSV 可按实际 GBK 编码完整解析为 3 条场地、6 条食堂、2 条菜品；23/18/24 列均无错位或截断，11 个稳定 ID 唯一，全部为 `pending_verification`，菜品价格和热量均为 null。相同输入重复导入结果一致；全部 JavaScript 语法、JSON 解析、WXML 标签平衡、资源状态闸门、安全限制闸门、无地点降级、7 日资源快照和 Git 差异格式通过。尚未在微信开发者工具、云环境或真机中验收。

2026-08-31 已完成培养方案 V1 设计资料本地检查：全部 draft JSON 可解析，规则/目录版本与状态存在，运动样本为 7 条，学院路/沙河均只有明确待核实占位，所有无可靠营养来源记录的 `estimatedKcal` 和精确营养值均为 null，运行代码没有引用 seed-data，Git 差异格式通过。Excel 因当前会话缺少 Spreadsheets artifact-tool 依赖尚未生成或验证。

2026-08-31 已完成培养页面第一阶段本地自动检查：全部 JavaScript 语法、JSON 解析、WXML 标签平衡、页面路由与组件注册、今日方案契约的 null/空数组及推荐/实际分离用例、preview 未调用云函数和 Git 差异格式均通过。尚未在微信开发者工具、WebView 渲染或真机中验收。

2026-08-31 已完成 V1 初始画像代码实现及本地自动检查：画像计算/来源、枚举拒绝、系统元数据忽略、身份字段拒绝、重大与普通变化、方案提示边界、动态过期、乐观并发条件、方案待调整边界、JavaScript、JSON、WXML、路由、组件、云函数入口与依赖、状态常量和 Git 差异格式均通过。尚未创建或修改线上集合、索引和权限，也未在微信开发者工具、云环境或真机中验收画像流程。

2026-08-30 已完成全部 JavaScript 语法检查、JSON 解析、WXML 标签平衡、页面路由与组件注册、云函数入口与依赖声明、身体字段服务端校验用例及 Git 差异格式检查；并检查首页不再引用具体身体字段、“我的画像”四态和返回刷新逻辑。用户已在微信开发者工具中人工验收首次引导、稍后填写、首次填写与保存、首页完成态激励语、“我的画像”档案展示、云端回填与修改、退出重进后的云端持久化、读取失败重试及页面无明显遮挡或裁切。真机适配、不同用户数据隔离、版本冲突和云端并发流程仍待验证。

第二轮代码已完成本地 JavaScript、JSON、WXML、路由与组件注册、云函数入口和状态常量检查；尚未部署新增云函数或集合，也未在微信开发者工具、云环境或真机中验证第二轮流程。

第二轮人工联调通过仅包含字段名的云端日志确认，`trainingPlan` 实际收到 `action`、`tcbContext` 和 `userInfo`；本地已将 `userInfo`、`tcbContext` 作为明确忽略的系统元数据，并保留各 action 业务字段白名单和身份字段拒绝。更新后的云函数尚待重新部署验证。

第一轮仍待验证：真机流程及不同屏幕适配；不同微信用户之间的数据隔离；`body_profiles._openid` 唯一性约束及仅管理端可读写权限；重复点击、版本冲突和多设备并发修改流程。

## 状态更新规则

- 每次完成功能、验证或重要决策后更新本文件。
- 只记录已经发生并能够确认的事实。
- “已写代码”不等于“已验证”；只有通过对应验证后才能勾选可运行状态。
- 自动化检查、微信开发者工具编译和真机验证应分别记录，不互相替代。
- 遇到阻塞时，在“当前问题”中写明原因和解除条件。
- 已解决的问题应从“当前问题”移除，并将结果记录到“已完成”或相关决策文档。

## 给下一位 Agent

开始工作前请阅读：

- `AGENTS.md`
- `README.md`（如果存在）
- `docs/REQUIREMENTS.md`（如果存在）
- 本文件
- 与当前任务直接相关的代码和产品资料

不要假设“计划完成”等于“代码已完成”。只有经过编译、自动化检查或明确人工验收的功能，才能记录为相应的已完成状态。
