# 开发日志

本文件按日期记录项目开发历史。新增日志时，将最新日期放在最上方；除修正事实错误外，不覆盖或删除既有记录。

## 2026-09-05（减脂餐图片与紧凑社区首页）

- 用户确认原图对应合一餐厅二楼，新增 food-xueyuan-option-bd51c94c2f.jpg（768×432、62,966字节）与稳定ID映射。根因是上一轮等待关联确认，未生成副本及映射，不是扩展名或构建忽略错误。蔓味轻食保持独立商家封面。
- 首页根据介绍区和底部导航实际位置统一计算200—230rpx卡高、18rpx间距；三张图片分别设置窗口裁剪焦点，已查看最小高度裁剪及新增餐品副本。失败路径记录及占位回退测试通过。
- 微信开发者工具CLI预览编译成功，本次测得主包1,701,288字节。社区图片、首页视口计算、社区页面、资源mock回归、WXML和路由检查通过；真机首屏与极小屏幕交互仍待人工验证。未修改数据库、云函数、训练方案或其他业务。

## 2026-09-05（社区图片）

- 重新扫描 21 张原图，只读核对原始运动 CSV 和稳定 ID；20 张已明确绑定，减脂餐照片因两个同名餐品等待用户确认。
- 生成 768×432 JPEG 副本及集中式图片映射，保留楼层聚合、分类与滚动逻辑；蔓味轻食和两个羽毛球馆分别使用自己的图片。最终图片逐张查看，源文件哈希未改变。
- 首轮预览包为 1,888,900 字节，接近容量边界后暂停并报告；用户明确选择 768×432 适度压缩，图片含占位总增量降至 1,223,948 字节。
- 17 个测试脚本和项目结构检查通过；CLI 编译预览成功，最终包体见 docs/community-image-package-info.json。多设备宽度实际测量与真机验收仍待完成。不修改数据库、云函数、训练规则、画像、打卡、FIFI 或日志业务，不上传云存储。

## 2026-09-03（北航资源 CSV dry-run 与统一读取边界）

### 完成

- 新增 UTF-8 BOM CSV 规范化与 dry-run：稳定 ID、内容哈希、日期、数组去重、价格区间、热量安全清空及餐厅外键均可检查。
- 新增三个校园资源集合的统一 repository 和事务 upsert 边界，但未创建集合、未连接执行导入。
- 培养方案从 repository 读取地点与餐食；读取失败降级为空资源，不阻止通用训练生成。
- 社区页面继续通过 service 读取，由 repository 请求同一资源域；状态文案使用“人工整理/待整理”。

### Dry-run

- 可转换：场地 9 条、餐厅/商家 10 条、菜品 4 条。
- 修复导入器错误排除蔓味轻食的名称特判；校外餐饮按 `external_merchant` 建模，允许空楼层并按校区加商家名称关联，当前无阻塞记录。
- 全部记录保持 `pending_verification`；没有创建集合或写入云数据库。

## 2026-09-02（FIFI 轻量陪伴第一版）

### 完成

- 新增 FIFI 页面及集中规则/纯计算模块，以既有当前方案日期和 `trainingPlan.getDailyCheckin` 三项打卡为唯一数据来源，确定性推导每日最多 10 分、总成长值、四个阶段、自然日连续天数与最新成就；不保存派生积分，不新增集合或云函数。
- 页面实现 loading、empty、error、success、失败重试和 `onShow` 返回刷新；三种 FIFI 状态使用原创本地 SVG 并提供加载失败占位，点击形象只切换温和文案，不影响积分。
- 接通“我的”“培养”“社区”的 FIFI 底部入口并注册路由；专项积分边界、去重、连续天数、成就、形象状态、页面重进刷新，以及项目结构检查已通过。当前历史范围受既有接口限制，仅为当前有效方案周期，尚未在微信开发者工具或真机中验收。

## 2026-09-02（社区页面第一版）

### 完成

- 新增社区首页、运动资源、饮食资源和校园分享页面，保持浅绿色、米白色和圆角卡片视觉；首页三张入口卡预留 `backgroundImage`，无图片时使用本地 CSS 渐变和半透明遮罩。
- 新增独立 `community-resource-service`，当前运动、饮食和帖子来源均为空数组；预留四种页面状态和三种核实状态，不接云数据库、地图、定位、第三方接口或 trainingPlan 方案资源。
- 运动和饮食页实现左侧分类切换、右侧资源卡片容器及明确空状态；论坛没有发布、点赞、评论等无效按钮。
- 接通“我的”和“培养”底部社区入口并注册四个页面路由；完成社区 JavaScript、JSON、WXML、路由、状态、核实边界及 Git 差异格式自动检查。尚未在微信开发者工具或真机中验收。

## 2026-09-02

### 完成

- 实现培养页每日三项打卡闭环：新增 `trainingPlan.getDailyCheckin/toggleDailyCheckin`，身份只取 `cloud.getWXContext().OPENID`，严格校验请求白名单、方案归属、北京时间日期、方案周期、dayPlan 和活动状态；`completedCount` 由云端计算，响应不返回 `_openid`。
- 新增独立 `cultivation_daily_checkins` 数据边界，使用确定性文档 ID、事务查询、revision 和部署时唯一复合索引防止重复及旧请求覆盖；不写入 `training_plans` 不可变快照，不保存画像、完整方案或健康敏感信息。
- 培养页三张主卡增加顶层手动完成/取消按钮，恢复日不自动完成；打卡加载前完成度保持加载态，保存时只锁定当前项，失败回滚并区分稳定错误，返回重进从云端回填。日期统一使用北京时间 `YYYY-MM-DD`。
- 本轮未开发健康记录、日志、趋势、成就、提醒、订阅消息或饮食记录。线上生效需创建并配置 `cultivation_daily_checkins`、唯一索引和仅管理端权限，并重新部署 `trainingPlan`。

## 2026-09-01

### 完成

- 修复画像目标变化后的方案闭环：画像保存响应增加 `profileVersion/changedFields/planRelevantChanged/planAction`，有当前方案且发生方案相关变化时可靠进入 `pending_confirmation`；“暂不调整”调用 `deferGeneration` 将旧快照标记 `outdated`，“调整方案”使用固定 requestId 和最新 profile/portrait 版本生成，成功后原子切换并归档旧方案。
- “我的方案”同时读取方案快照目标和当前画像目标，仅在二者不同时显示差异提示，不用当前画像覆盖旧方案内容；pending 不再显示为“当前有效”。“培养”不再把 pending、outdated 或调整失败后的旧方案当作当前画像的可执行任务。
- `trainingPlan.getStatus/getActivePlan` 增加画像版本落后状态自校正，历史 active 状态与当前画像不一致时事务性转为 outdated；页面继续使用 onShow 请求号避免旧 Promise 覆盖新方案。
- 为“我的方案”的旧结构方案卡片补充“生成新版方案”入口；沿用现有调整生成、幂等和成功后归档旧方案流程，不把旧 7 日快照伪装成新 4 周方案。
- 实现培养方案 V1 第二轮开发测试链路：新增集中 `product_draft` 规则和安全闸门，以单次不可变快照生成五目标差异化的 4 周概要、四阶段周结构及 28 天训练/轻活动/恢复安排；无执行反馈时第 3 周保持，不启用热量、体重速度、精确蛋白质、负荷、恢复小时数或睡眠时长等待审核参数。
- `trainingPlan` 幂等有效方案查询增加画像与安全版本，激活事务重新核对身体档案、画像和安全筛查；新方案成功后才归档旧方案，失败继续保留 `currentPlanId`。总体与每日页面均读取同一 plan content，旧结构快照仅降级展示。
- “我的方案”补充总体摘要、周期进度、四周概览、训练/饮食/作息原则、安全与 draft 状态；“培养”展示同一快照的当日切片和开发阶段免责声明。北航资源状态闸门未改变，pending 数据不成为明确推荐。
- 修复安全信息保存闭环：统一 `saveSafetyScreening` action 契约，严格区分未知 action、未知字段和安全信息不完整，前端补充确认校验、重复提交保护、成功返回及无上一页时返回画像页；版本冲突、网络和服务端失败均保留当前输入。
- `userPortrait` 仅忽略已确认的 `userInfo`、`tcbContext` 系统元数据，继续拒绝前端身份字段并只使用 `cloud.getWXContext().OPENID`；诊断日志只记录 action、event 键名、错误码和阶段。
- 安全筛查自动检查覆盖四类枚举、资格判断、缺项/未确认、重复点击、保存返回、重新进入回填、未知 action/字段、身份字段、系统元数据、失败保留输入及无页面栈回退；JavaScript、JSON、WXML、路由、组件、云函数依赖和差异格式检查通过。更新后的 `userPortrait` 仍需重新部署并完成开发者工具闭环验收。

## 2026-08-31

### 完成

- 完成培养方案候选规则 `fitflight-cultivation-v1-draft.2` 的纯设计交付：补充当前临时生成器审计、权威来源采用边界、现有字段映射与安全输入缺口、完整安全闸门、五目标差异、配置化 4 周周期、总体/每日同源快照结构、参数审核表和 16 个匿名测试画像；本轮未修改运行代码、云函数、数据库或页面。
- 为 `trainingPlan` 方案生成增加分阶段安全错误日志，区分 `generate_content` 与 `activate_plan`；仅记录错误码和截断后的错误消息，不记录 OpenID、画像、方案内容或完整请求，用于定位云端 `PLAN_GENERATION_FAILED`。
- 修复方案激活时云数据库将对象内容展开写入 `content.dailyPlans`、但生成中记录的 `content` 为 `null` 而触发 `-502001` 的问题；激活事务改为使用数据库 `set` 命令整体替换方案快照，不需要删除历史失败记录。
- 接入北航培养资源 CSV 导入边界 `scripts/import-buaa-resources`，生成云函数内不可变版本 `buaa-resource-pending.1`；导入 3 条运动场地、6 条食堂和 2 条菜品，全部保持 `pending_verification`。导入器校验表头、列数、稳定 ID、食堂引用、热量来源和历史版本不可覆盖，并记录源文件编码、SHA-256 与记录数；原始 CSV 实际为 GBK 而非标称 UTF-8。
- 实现资源状态闸门和匹配降级：只有 `verified`、存在 `verifiedAt` 且未过期的数据可成为具体地点/菜品推荐；当前待核实资源只触发明确提示，地点固定降级为“暂无已核实地点”，不展示待核实价格、营业规则或热量。
- 将保守的通用健康管理方案接入 `trainingPlan` 唯一生成入口，按用户校区、目标、运动偏好、场地能力标签、餐别和食物分类标签执行资源匹配；存在或不确定运动限制时拒绝生成并提示咨询专业人员。方案保存资源版本及逐日资源快照，后续资源更新不会动态改变旧方案。
- 培养页改为读取当前有效方案，展示今日内容、推荐时间、地点、地点状态、饮食建议、待核实提示和“仅供健康管理参考，医疗问题请咨询专业医生”；无可靠营养来源时不展示推测热量。
- 用户画像增加校区字段，作为资源优先匹配输入；旧画像缺少该字段时按 `unknown` 安全兼容。
- 完成本轮本地自动检查：全部 JavaScript 语法、JSON 解析、WXML 标签平衡和 Git 差异格式通过；重复导入输出一致，11 个资源 ID 全部唯一，状态均为 `pending_verification`，安全闸门、无地点降级、空热量/价格及 7 日资源快照用例通过。尚未在微信开发者工具、云环境或真机中验收。
- 完成“培养方案 V1”规则规格和数据字典设计，记录中国居民膳食指南（2022）、全民健身指南、健康中国行动（2019—2030年）和体重管理指导原则（2024年版）的官方来源、适用边界、FitFlight 采用摘要及核实日期；未复制大段原文，也未声称已完成专业审查。
- 设计 `health_rule_sets`、`exercise_catalog`、`campus_venues`、`meal_pattern_catalog`、`campus_food_options`，明确健康规则与北航资源解耦、资源缺失降级和历史方案快照不可变。
- 新增版本控制中的 draft/preview JSON：1 个规则集、7 种通用运动、3 个膳食结构模板及两校区的场地/食堂待调查占位记录；无营养来源时所有热量和精确营养值保持 null，且未接入任何运行代码或云函数。
- 完成 `docs/北航培养资源调查表说明.md`，定义工作簿六类工作表的字段用途、可空性、示例、下拉值和禁止填写条件。
- 新增独立“培养”页面及首页底部入口，完成日期/周期/完成度、健康记录/我的日志入口、锻炼/饮食/作息卡片，以及 `loading`、`empty`、`ready`、`rest`、`error` 五种页面状态；未新增健康记录或日志假路由。
- 集中定义今日方案 `plan`、独立 `dailyRecord` 和组合 `displayModel` 契约，保留 `plannedExercise/exerciseRecord`、`recommendedMeals/actualMeals`、`recommendedSleep/actualSleep` 的不可覆盖边界；实际饮食存在时支持默认显示实际记录并切回原推荐。
- 新增默认关闭的集中 preview 数据与今日方案 service 接口。默认正式状态展示“今日暂无培养方案”；preview 热量为 `null`，内容明确标注为页面预览，不调用 `trainingPlan`、`userPortrait` 或其他云函数，也不写数据库。
- 健康记录、我的日志和记录饮食当前只显示明确的后续接入提示；锻炼勾选仅为只读视觉占位，没有本地或云端完成逻辑。
- 完成本轮 JavaScript、JSON、WXML 标签、路由、组件、契约 null/空数组、推荐/实际分离、preview 云函数隔离和 Git 差异格式检查。

- 实现 V1 初始用户画像代码：新增 `userPortrait` 云函数、`user_portraits` 一用户一条当前画像模型和 `portraitVersion` 乐观并发控制；身份仅来自 `cloud.getWXContext().OPENID`。
- BMI 和体重目标差仅由云端读取 `body_profiles` 后计算；画像关联云端读取的 `profileVersion`，不接受前端提交计算值、身体档案版本或归属字段，响应不返回 `_openid` 和内部变化基线。
- 新增集中画像枚举、严格请求白名单和 `portrait-change-v1` 变化策略；运动偏好支持固定多选且 `unsure` 互斥，重大变化阈值未散落到页面。
- 身体资料首次完整保存后改为显示初始画像引导；会话内可暂时跳过，画像失败不回滚身体资料，且不再从身体资料页调用培养方案生成器。
- “我的画像”支持基础资料摘要、画像生成与修改、云端计算指标、训练与安全条件、动态 `needs_regeneration`、错误重试和版本冲突提示；未生成体脂率、骨骼肌量、骨量、骨密度或医学判断。
- `trainingPlan` 新增无业务参数的 `setPortraitAdjustmentPending`，仅在最新画像且已有 active/outdated 方案时复用 `pending_confirmation`；不调用生成器、不清除 `currentPlanId`、不覆盖旧方案。
- 新增 `docs/USER_PORTRAIT_DATA.md`，记录集合、权限、索引、接口和部署顺序；未自动修改云环境。
- 完成本地画像功能用例与静态检查：BMI/体重目标差、来源与年龄排除、严格枚举和请求白名单、系统元数据忽略、身份及伪造计算字段拒绝、重大/普通变化、方案提示边界、动态过期、乐观并发条件、方案待调整不跨越生成器边界，以及 JavaScript、JSON、WXML、路由、组件、云函数依赖、状态常量和 Git 差异格式均通过。

### 决定

- CSV 只作为人工维护和离线导入来源，运行时只读取随云函数部署的版本化 JSON；相同版本内容不得被覆盖，新增或变更数据必须使用新的 `resourceDataVersion` 和输出文件。
- 导入动作本身永远不会把记录提升为 `verified`；核实状态只能在具有可靠来源、核实日期和明确审核动作后单独变更。
- “培养”作为独立每日执行展示页，不替换现有“我的方案”生成状态页；周期方案由 `plan` 提供，用户实际执行后续通过独立 `dailyRecord` 保存，页面只消费组合后的 `displayModel`。
- 正式规则接入前 service 默认返回 `empty`，不以 preview 伪装生成结果；正式生成规则、热量算法和持久化完成记录均留待后续阶段。

### 待验证

- 培养方案 V1 的安全闸门、周期增量、目标差异、运动目录和膳食模板仍需医生、营养师、体育教师及北航主管部门审查，当前只能保持 draft。
- 在微信开发者工具中验收培养页五态、Skyline/WebView、不同屏幕宽度、顶部与底部安全区、长饮食名称换行、较多锻炼项目滚动和连续点击提示。
- 创建 `user_portraits`、配置 `_openid` 唯一索引和仅管理端可读写权限后，部署 `userPortrait` 与 `trainingPlan`，完成首次生成、会话跳过、画像修改、身体资料变化后过期、并发冲突及方案待调整流程。
- 通用方案规则仍是 `draft`，需医生、营养师和体育教师审核；当前输出仅供健康管理参考，不是医疗建议。

## 2026-08-30

### 完成

- 修复 `trainingPlan.getStatus` 将微信运行环境元数据误判为未知业务字段的问题：云端键名日志确认实际字段为 `action`、`tcbContext`、`userInfo`，其中 `tcbContext` 和 `userInfo` 被明确忽略且不参与身份判断；未知业务字段及 `openid`、`openId`、`_openid`、`ownerOpenId`、`userId` 仍返回 `INVALID_PARAM`。
- 为方案云函数参数白名单提取可独立验证的校验模块，并补充纯请求、携带 `userInfo`、未知字段、身份字段和其他 action 白名单用例；校验拒绝时仅记录 `Object.keys(event)` 和最终识别的 `unknownFields` 字段名列表，未记录字段值或完整请求。
- 实现第二轮方案状态链路：集中定义 `none`、`pending_confirmation`、`generating`、`active`、`outdated`、`archived`、`generation_failed`、`generator_not_configured`，页面不直接硬编码状态值。
- 新增独立 `trainingPlan` 云函数、前端 service 和可替换 `plan-generator.js`；所有身份由 `cloud.getWXContext().OPENID` 获取，方案读取、状态更新和归档均限定当前用户，响应不返回 OpenID。
- 当前生成器不包含正式训练规则，稳定返回 `PLAN_GENERATOR_NOT_CONFIGURED`；生成失败不会回滚身体档案，新方案成功前不会删除或覆盖旧方案。
- `saveBodyProfile` 增加集中方案相关字段判断，成功响应包含 `planRelevantChanged`、字段名列表和当前方案摘要；无实际变化时仍不增加版本、不触发询问。
- 身体信息页增加首次完成后的生成询问和相关字段修改后的调整询问；“暂不调整”保留旧方案并标记 `outdated`，请求期间禁用重复操作并复用 requestId 处理网络重试。
- 新增“我的方案”页面，覆盖暂未生成、生成中、有效、过期、生成失败和规则未配置的最小展示及重试入口；未扩展打卡、日历、成就或其他功能。
- 新增方案集合、字段、索引、权限和部署顺序说明；未自动创建线上集合、索引或修改数据库权限。
- 将“我的”首页身体档案完成态改为固定激励语和完成状态，不再读取到页面数据后构造或展示身高、体重、目标体重、活动水平，也不提供完成态快捷修改入口；未完成、加载、失败及首次引导逻辑保持云端状态驱动。
- 补齐原先只有入口外观但没有页面和路由的“我的画像”页面；复用统一 `getBodyProfile` service 展示身体档案 `loading`、`incomplete`、`complete`、`error` 四态，并在每次 `onShow` 时重新读取云端，确保修改返回后刷新。
- “我的画像”仅展示已有的性别、出生日期、身高、当前体重、选填目标体重和日常活动水平，并通过“查看/修改”进入现有身体信息页；未新增集合、字段、缓存或读取接口。
- 在现有 `saveBodyProfile` 云函数中兼容增加当前用户身体档案读取、完成状态判断、版本化保存、无变化检测和稳定错误响应；身份仅来自 `cloud.getWXContext().OPENID`，页面不能指定用户或记录。
- 身体信息页面改为先从云端读取，读取成功后才显示并回填表单；支持修改、重复提交拦截、保存失败提示和 `PROFILE_VERSION_CONFLICT` 后重新加载。
- “我的”页面增加身体档案 `loading`、`incomplete`、`complete`、`error` 四态；完成态最终调整为只展示固定激励语和完成状态，不展示任何具体身体数据。
- 增加首次身体信息引导组件；“稍后再说”只在当前小程序会话内生效，完成状态始终以云数据库为准。
- 完成 JavaScript 语法、JSON 解析、WXML 标签平衡、页面与组件注册、云函数入口与依赖、字段校验用例及 Git 差异格式检查；项目当前没有可用的 ESLint 命令。
- 用户已在微信开发者工具中人工验收首次进入引导、稍后填写、首次填写与保存、首页完成态激励语、“我的画像”身体档案展示、查看回填与修改、退出重进后的云端数据持久化、读取失败重试及页面无明显遮挡或裁切。

### 决定

- 正式方案规则完成前不生成 mock 训练、饮食或医疗内容；后续规则只通过 `cloudfunctions/trainingPlan/plan-generator.js` 替换。
- 使用 `training_plan_states` 保存每用户当前状态和 `currentPlanId`，使用 `training_plans` 保存不可覆盖的生成记录；生成成功切换方案时使用事务，失败保留旧 `currentPlanId`。
- 继续复用 `body_profiles` 集合、`saveBodyProfile` 云函数和现有身体信息页面，不增加新的后端服务或技术框架。
- 目标体重保持选填；旧记录缺少 `profileVersion` 时按版本 1 兼容，后续有效修改递增版本。
- 身体档案只通过云函数读写；云开发控制台应将集合权限设置为仅管理端可读写，并为 `_openid` 创建唯一索引。

### 待验证

- 重新部署 `trainingPlan` 后确认携带已确认系统元数据的 `getStatus` 正常，且主动加入未知业务字段时仍返回 `INVALID_PARAM`。
- 部署 `trainingPlan` 和更新后的 `saveBodyProfile`，创建方案集合与索引后，在微信开发者工具覆盖首次暂不生成、首次生成、规则未配置、相关字段修改、暂不调整、失败重试和重复点击流程。
- 正式生成器尚未配置，因此调整成功、旧方案原子归档及生成过程中档案再次修改只能在后续提供受控测试生成器后做云端联调；当前仅完成代码路径和静态检查。
- 在真机上验证首次引导、身体档案填写与修改、不同屏幕适配及网络异常恢复。
- 验证重复点击、`PROFILE_VERSION_CONFLICT` 和多设备并发修改流程。
- 在云开发控制台确认 `body_profiles` 集合唯一索引和仅管理端可读写权限规则后，使用不同微信用户验证彼此不能读取或修改档案。

## 2026-08-28

### 完成

- 完善用户登记后的“我的”首页：增加用户资料空状态、我的画像、成长趋势、我的方案、横向成就徽章和设置与隐私入口；详情页、身体资料填写及入口跳转暂未开发。
- 完成“我的”页面相关 JavaScript 语法、JSON 解析和 WXML 标签平衡检查；尚未进行微信开发者工具编译或真机验证。
- 在原生小程序首页实现头像和昵称登记：首次进入检查登记状态，未登记时显示资料表单，登记后进入用户初始界面，并支持重新设置。
- 使用原生 `chooseAvatar` 和昵称输入能力获取用户选择的微信资料，本地保存头像文件与资料；未新增云开发资源或数据库结构。
- 完成 JavaScript 语法、JSON 解析、WXML 标签平衡及用户资料本地存储状态检查；尚未进行微信开发者工具编译或真机验证。
- 准备微信开发者工具和 VS Code 开发环境。
- 在工作区配置 Codex 协作规则 `AGENTS.md`。
- 建立项目状态文件 `PROJECT_STATES.md`。
- 确认工作区已有需求文档 `FitFlight.docx`、网页 Demo `fitflight-fifi-demo.html` 和项目展示 PPT `微信小程序pptx.pptx`。

### 决定

- 使用原生微信小程序进行前端开发。
- 使用微信云开发作为当前后端技术方向。
- 使用 `PROJECT_STATES.md` 记录当前状态，使用本文件按日期保留开发历史。
- 只有经过编译、自动化检查或明确人工验收的内容，才记录为相应的已验证状态。

### 遇到的问题

- 页面清单和数据库结构尚未确定。
- 尚未建立可运行的小程序项目，暂时不能进行编译和真机验证。
- 小程序主体类型、核心计算规则及可能涉及的服务类目仍需确认。

### 下一步

- 创建原生微信小程序云开发项目。
- 确定页面清单和数据库结构。
- 创建页面骨架，并开始将网页 Demo 首页转换为小程序页面。
